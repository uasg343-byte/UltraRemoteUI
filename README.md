# Ultra Remote UI

A standalone Android Jetpack Compose UI prototype.

## Build
Open this folder in Android Studio, let Gradle sync, then use:
Build -> Build APK(s)

The app has no networking and no external-device/game control. It only responds locally to UI taps.
